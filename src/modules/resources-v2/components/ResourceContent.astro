---
import SearchBar from './ResourceContent/SearchBar'
import ResourceGrid from './ResourceContent/ResourceGrid'
---

<div class='flex flex-col gap-12'>
  <div class='flex flex-col gap-8'>
    <div
      class='flex flex-col md:flex-row md:items-center justify-between gap-6'
    >
      <div class='flex flex-col gap-2'>
        <h2
          class='text-4xl font-extrabold text-white tracking-tight sm:text-5xl'
        >
          Recursos de ShareIT
        </h2>
        <p class='text-slate-400 text-lg max-w-2xl'>
          Colección curada de las mejores herramientas y recursos para el
          desarrollo web, diseño, DevOps y más.
        </p>
      </div>

      <div class='flex items-center gap-4'>
        <button
          class='px-6 py-3.5 text-white font-bold rounded-2xl transition-all duration-300 shadow-xl shadow-blue-500/20 hover:shadow-blue-500/40 active:scale-95 flex items-center gap-2.5'
          style='background: var(--brand-gradient);'
        >
          <span class='text-2xl leading-none'>+</span>
          Sugerir recurso
        </button>
      </div>
    </div>

    <div class='flex flex-col md:flex-row md:items-center gap-4'>
      <SearchBar client:load />
    </div>
  </div>

  <ResourceGrid client:load />
</div>
